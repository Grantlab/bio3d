

configuration <<- list(
  
  db = list(
    use = FALSE,
    host = "localhost",
    dbname = "test",
    username = "root",
    password = ""),

  hmmer = list(
    local = FALSE,
    exefile = "phmmer",
    pdbseq = "data/pdb_seqres.txt"
    )
  )
  
    

  
    
  
                 
